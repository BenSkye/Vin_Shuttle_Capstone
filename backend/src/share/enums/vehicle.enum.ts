export enum VehicleOperationStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    DELETED = 'deleted'
}

export enum VehicleCondition {
    AVAILABLE = 'available',
    IN_USE = 'in-use',
    MAINTENANCE = 'maintenance'
}