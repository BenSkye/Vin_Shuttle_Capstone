export enum VehicleOperationStatus {
  PENDING = 'pending',
  RUNNING = 'running',
  CHARGING = 'charging',
}

export enum VehicleCondition {
  AVAILABLE = 'available',
  IN_USE = 'in-use',
  MAINTENANCE = 'maintenance',
}
